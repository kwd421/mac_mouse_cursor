# Cape Forge App Store Connect Metadata

## Existing URLs

- Support URL: `https://kwd421.github.io/mac_mouse_cursor/`
- Privacy Policy URL: `https://kwd421.github.io/mac_mouse_cursor/privacy.html`
- Korean Support URL: `https://kwd421.github.io/mac_mouse_cursor/ko/`
- Korean Privacy Policy URL: `https://kwd421.github.io/mac_mouse_cursor/ko/privacy.html`

## App Name

- Cape Forge

## Recommended Final Metadata

- App Name: `Cape Forge`
- Subtitle: `Convert CUR and ANI to CAPE`
- Keywords: `cursor,cape,mousecape,ani,cur,converter,export,theme,macos,windows`
- Support URL: `https://kwd421.github.io/mac_mouse_cursor/`
- Privacy Policy URL: `https://kwd421.github.io/mac_mouse_cursor/privacy.html`

## Subtitle Candidates

- Convert CUR and ANI to CAPE
- Cursor Pack to CAPE Converter
- Export Mousecape-Compatible CAPE

## Keywords

Recommended keyword set:

- cursor,cape,mousecape,ani,cur,converter,export,theme,macos,windows

Alternative keyword set:

- cursor pack,cape file,mousecape,ani,cur,convert,export,theme,utility,mac

## Final English Description

Cape Forge is a macOS utility that converts Windows cursor files and cursor packs into `.cape` files.

Choose a folder that contains `.ani` or `.cur` files, review the mapped cursor roles, adjust individual roles if needed, and export a Mousecape-compatible CAPE file.

Cape Forge reads only the folders and files you choose. It does not require network access and exports only to the location you select.

Key features:

- Reads `.ani` and `.cur` cursor files
- Automatically maps common cursor roles
- Lets you preview and replace individual cursor roles
- Exports a Mousecape-compatible `.cape` file
- Supports multiple app languages

## Final Korean Description

Cape Forge는 Windows 커서 파일과 커서팩을 macOS용 `.cape` 파일로 변환하는 유틸리티입니다.

`.ani` 또는 `.cur` 파일이 들어 있는 폴더를 선택하면, 앱이 역할별로 자동 매핑을 시도하고 미리보기를 보여줍니다. 필요하면 특정 역할의 커서를 직접 바꾼 뒤 Mousecape 호환 CAPE 파일로 내보낼 수 있습니다.

Cape Forge는 사용자가 직접 선택한 폴더와 파일만 읽습니다. 네트워크 접근이 필요하지 않으며, 내보내기도 사용자가 지정한 위치에만 수행합니다.

주요 기능:

- `.ani`, `.cur` 커서 파일 읽기
- 자주 쓰는 커서 역할 자동 매핑
- 역할별 미리보기 및 개별 커서 변경
- Mousecape 호환 `.cape` 파일 내보내기
- 여러 언어 지원

## Screenshot Checklist

### Screenshot 1: Main workflow

- Show the sidebar with mapped cursor roles
- Show one selected role with both preview panes visible
- Keep the export area visible at the bottom

Suggested caption:

- Convert Windows cursor files into CAPE files on macOS

### Screenshot 2: Per-role editing

- Show a role with a changed cursor file
- Keep the preview large enough to read clearly
- If possible, show a cursor pack with visible animation or clear styling differences

Suggested caption:

- Preview and replace individual cursor roles before export

### Screenshot 3: Additional cursors

- Expand the `Additional Cursors` section
- Show that uncommon cursor states can also be reviewed or overridden

Suggested caption:

- Adjust additional cursor states when needed

### Screenshot 4: Export flow

- Show author field and export button
- Keep the UI clean with a valid loaded pack

Suggested caption:

- Export a Mousecape-compatible `.cape` file

## Notes

- In App Store metadata, describe Mousecape only as a compatible target format or compatible app.
- Avoid wording that implies official affiliation with Mousecape.
- Use the archive built from Xcode Organizer for final screenshots and validation.
